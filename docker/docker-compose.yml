version: "3.9"

services:
  monitor:
    build: ./docker
    container_name: sports_picks_monitor
    env_file: .env
    volumes:
      - ./app/data:/app/data
      - ./app/logs:/app/logs
    network_mode: "host"    # so it can reach Ollama on 192.168.0.111:11434
    restart: unless-stopped
